# MongoDB 连接串（生产环境请在 Vercel 项目环境变量中配置）
MONGODB_URI="mongodb://root:your-password@dbconn.sealosbja.site:31728/?directConnection=true"

# 数据库名称
MONGODB_DB="shouhoufufeituiguang"

# 可选：写接口鉴权密钥（配置后，POST/DELETE 需在 Header 中携带 x-api-key 或 Bearer Token）
API_WRITE_KEY=""

# 可选：前端自动附带的写密钥（仅用于内网场景，需与 API_WRITE_KEY 保持一致）
VITE_API_WRITE_KEY=""
